# 待办事项（TODO）

## 策划

可立刻开始：
- 抽象水管：把高柱子和矮柱子抽象成 2 个单独的场景，然后再实例化到 Main.tscn 中。
- 抽象云彩：把云抽象成单独场景以后实例化 6 个到场景中。
- 抽象地面：把地面节点树抽象成单独场景文件，实例化一次。
- 抽象空气墙：把空气墙抽象成单独场景文件，实例化2次。
- 按照先后渲染顺序重新排序节点树
- 调优游戏手感：调整角色起跳高度和移动速度
- 调整相机（Camera2D）的 limit 属性，使得相机不完全跟随角色，跳起的时候相机延缓向上移动，使得角色在相机画面中位置相对靠上，
	角色向左移动的时候相机不会向左移动。

排队中：
- 制作动画；等待程序制作几张角色动画素材

已完成（近期，太久远的会删除）：
- 场景边缘添加空气墙 
- 调整节点父子关系
- 调整地面碰撞位置（建议：调整 direction 数值而不是 position 移动变换）
- 调整场景缩放倍数，需要修改 Camera2D 的 zoom 缩放属性

---

## 程序

可立刻开始：
- 绘制角色动画帧

排队中：
- 等待策划完成相机 limit 属性，编写角色向左移动会卡在屏幕左侧边缘的代码
- 编写二段跳逻辑（默认禁止，当未来玩家吃到特殊糖豆以后启用）

已完成（近期，太久远的会删除）：
- 给跳起和坠落施加不同的重力加速度
- 编写玩家移动惯性逻辑（停止按 AD 以后惯性影响继续移动并逐渐减速）
